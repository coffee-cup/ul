// **********************************************
// This whole file should pass lexing and parsing
// **********************************************

int func() {
    // Test variable declarations
    int a;
    float b;
    char c;
    string d;
    boolean e;
    void f;
    int [2] g;
    char[256] h;

    // Test statements
    ;
    foo(a, b, c);
    bar();
    hello(1);
    world(23, 100);
    if (true) {
        ; ; ; ; x = 3;
    }

    if (false) {
        while (g[1]) {
            while (foo(1, 23, bar())) {}
        }
    } else {
        print "Hello";
        println 123;
        if ((2 + 3) < 3) {
            return 'a';
        } else {
            return "balloon";
        }
        return (foo());
    }

    // Test expressions
    g[2];
    g[1999];
    c[2];
    foo();
    bar(1, 2, 3);
    g;
    a; b; c;
    (foo(100, bar(), charlie("asdf", g[100])));

    // Test operators
    a = 1 + 2;
    a = 1 - 2;
    a = 1 * 2;
    b = 1 < 2;
    b = 1 == 2;
    x = x * f(x + b(a * (2 - test)));

    // Test literals
    "hello"; "";
    'a'; ' ';
    1; 0; 100;
    1.; 1.0; 0.34; 1.45;
    true;
    false;

    // Test assignment
    a = 2;
    b = 23.23;
    b = 23.;
    c = 'a';
    d = "hello";
    d = "";
    e = true;
    e = false;
    g[0] = 0;
    g[1] = 10;
}

int foo(int a, float b, char c, string d, boolean e, void f) {}
float floatFn() {}
boolean boolFn() {}
int[3] arrFn() {}
int    [45]    fn() {}
void a() {}
void main() {}
