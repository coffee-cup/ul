int ifFn () {
    int x;
    int a;
    int b;
    int y;
    int c;
    int hello;
    int world;
    int t;
    int h;

    if (cmp(x, b)) {}
    if (cmp(1, 1.1)) {}
    if (x + 2 < 40) {}
    if (true) {} else {}
    if (a < b) {} else { x = 1; }

    if (a < (x + (x + ((x * 2) - (5 - y))))) {
        ;
    } else {
        ;
    }

    if (a < b) {
        if (b < c) {
            ;
        }
        else {
            x = 2;
            if (true) {
                if (hello == world) {
                    ;
                }
            }
        }
    }

    return 2;
}

void whileFn() {
    int x;
    while(true) {}
    while(false) {}
    while     (x == 2) {;}
}

void printFn() {
    int a;
    int[2] x;
    print (a + 1);
    print a;
    print "hello";
    print 'a';
    print x[2];
    print 2.3;
    print true;
    print false;

    println (a + 1);
    println a;
    println "hello";
    println 'a';
    println x[2];
    println 2.3;
    println true;
    println false;
}

void main() {}

boolean cmp(float x, float y) { return x < y; }
