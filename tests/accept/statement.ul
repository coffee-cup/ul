int ifFn () {
    int x;

    if (x + 2 < 40) {
        ;
    }

    if (true) {
        ;
    } else {
        ;
    }

    if (a < b) {
        ;
    }
    else {
        x = 1;
    }

    if (a < (x + (x + ((x * 2) - (5 - y))))) {
        ;
    } else {
        ;
    }

    if (a < b) {
        if (b < c) {
            ;
        }
        else {
            x = 2;
            if (true) {
                if (hello == world) {
                    ;
                }
            }
        }
    }

    if ((6 - 2) < (y + b * (t - y)) == (h * (b * (a)))) {
        ;
    } else {
        ;;;
        if (true) {
            ;
        } else {
            if (false) {
                if (hello == world) {
                    if (a < b) {
                        return i;
                    }
                }
                else
                    {
                        return a + (c * 22);
                    }
            }
        }
    }
}

void whileFn() {
    while(true) {}
    while(false) {}
    while     (x == 2) {;}
    while (((((((((((((x - 3) + 2) - 4))))))))))) {}
    while  (  (6 - 2) < (y + b * (t - y)) == (h * (b * (a)))) {
        while(true) {
            while(false) {
                ;
            }
        }
    }
}

void printAndReturn() {
    print (a + 1);
    print a;
    print "hello";
    print 'a';
    print x[2];
    print 2.3;
    print true;
    print false;

    println (a + 1);
    println a;
    println "hello";
    println 'a';
    println x[2];
    println 2.3;
    println true;
    println false;

    return (a + 1);
    return a;
    return "hello";
    return 'a';
    return x[2];
    return 2.3;
    return true;
    return false;
}

void main() {}
